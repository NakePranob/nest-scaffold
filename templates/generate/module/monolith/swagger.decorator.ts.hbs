import { applyDecorators } from '@nestjs/common';
import { ApiBearerAuth, ApiBody, ApiOperation, ApiParam } from '@nestjs/swagger';
import {
  ApiConflictError,
  ApiDefaultErrors,
  ApiNotFoundError,
  ApiProtectedErrors,
} from '{{commonFromNested}}/swagger/decorators/api-error-responses.decorator';
import {
  ApiEmptySuccessResponse,
  ApiPaginatedSuccessResponse,
  ApiSuccessResponse,
} from '{{commonFromNested}}/swagger/decorators/api-success-response.decorator';
import { {{errorPrefix}}_ERRORS } from '../{{entityFile}}-error-catalog';
import { Create{{entityName}}Dto } from '../dto/create-{{entityFile}}.dto';
import { Update{{entityName}}Dto } from '../dto/update-{{entityFile}}.dto';
import { {{entityName}}ResponseDto } from './{{entityFile}}-response.dto';

export function ApiCreate{{entityName}}Docs() {
  return applyDecorators(
    ApiOperation({ summary: 'Create {{entityFile}}' }),
    ApiBody({ type: Create{{entityName}}Dto }),
    ApiSuccessResponse({{entityName}}ResponseDto),
    ApiConflictError(
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_ALREADY_EXISTS.code,
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_ALREADY_EXISTS.message,
    ),
    ApiDefaultErrors(),
  );
}

export function ApiFind{{pascalName}}Docs() {
  return applyDecorators(
    ApiBearerAuth(),
    ApiOperation({ summary: 'Search {{name}} with pagination' }),
    ApiPaginatedSuccessResponse({{entityName}}ResponseDto),
    ApiProtectedErrors(),
    ApiDefaultErrors(),
  );
}

export function ApiFind{{entityName}}ByIdDocs() {
  return applyDecorators(
    ApiBearerAuth(),
    ApiOperation({ summary: 'Get {{entityFile}} by ID' }),
    ApiParam({ name: 'id' }),
    ApiSuccessResponse({{entityName}}ResponseDto),
    ApiNotFoundError(
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_NOT_FOUND.code,
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_NOT_FOUND.message,
    ),
    ApiProtectedErrors(),
    ApiDefaultErrors(),
  );
}

export function ApiUpdate{{entityName}}Docs() {
  return applyDecorators(
    ApiBearerAuth(),
    ApiOperation({ summary: 'Update {{entityFile}}' }),
    ApiParam({ name: 'id' }),
    ApiBody({ type: Update{{entityName}}Dto }),
    ApiSuccessResponse({{entityName}}ResponseDto),
    ApiNotFoundError(
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_NOT_FOUND.code,
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_NOT_FOUND.message,
    ),
    ApiProtectedErrors(),
    ApiDefaultErrors(),
  );
}

export function ApiDelete{{entityName}}Docs() {
  return applyDecorators(
    ApiBearerAuth(),
    ApiOperation({ summary: 'Delete {{entityFile}}' }),
    ApiParam({ name: 'id' }),
    ApiEmptySuccessResponse(),
    ApiNotFoundError(
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_NOT_FOUND.code,
      {{errorPrefix}}_ERRORS.{{errorPrefix}}_NOT_FOUND.message,
    ),
    ApiProtectedErrors(),
    ApiDefaultErrors(),
  );
}
