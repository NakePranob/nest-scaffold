# Tech Stack: {{projectName}}

> Status: generated by `@nakedev/nest-scaffold`
> Stack baseline: {{stackLabel}}

## Stack Overview

| Layer | Technology |
|-------|-----------|
| Language | TypeScript `{{stack.typescript}}` |
| Runtime target | Node.js >= 20 |
| Framework | NestJS `{{stack.nestCommon}}` |
| HTTP Adapter | Fastify (`@nestjs/platform-fastify` `{{stack.nestPlatformFastify}}`) |
| Database | PostgreSQL |
| ORM | TypeORM (`@nestjs/typeorm` `{{stack.nestTypeorm}}`) |
{{#if auth}}| Auth | JWT + Passport (`@nestjs/jwt` `{{stack.nestJwt}}`, `@nestjs/passport` `{{stack.nestPassport}}`) |{{/if}}
{{#if swagger}}| API Docs | Swagger (`@nestjs/swagger` `{{stack.nestSwagger}}`) |{{/if}}
| Validation | `class-validator` `{{stack.classValidator}}` + `class-transformer` `{{stack.classTransformer}}` |
| Testing | Jest `{{stack.jest}}` + `@nestjs/testing` `{{stack.nestTesting}}` |

## Scaffolded Capabilities

- Response envelope: `always enabled by the CLI`
- Pagination helpers: `always enabled by the CLI`
- TypeORM + PostgreSQL baseline: `always enabled by the CLI`
- Example Users module: `{{#if usersModule}}enabled{{else}}disabled{{/if}}`
- Auth module: `{{#if auth}}enabled{{else}}disabled{{/if}}`
- Swagger docs: `{{#if swagger}}enabled{{else}}disabled{{/if}}`
- Docker setup: `{{#if docker}}enabled{{else}}disabled{{/if}}`
- E2E scaffold: `{{#if e2e}}enabled{{else}}disabled{{/if}}`
- Module versioning: `{{#if moduleVersioning}}enabled (default: `{{defaultModuleVersion}}`){{else}}disabled{{/if}}`

## Key Packages

| Package | Version | Purpose |
|---------|---------|---------|
| `@nestjs/common` | `{{stack.nestCommon}}` | Core Nest decorators and providers |
| `@nestjs/core` | `{{stack.nestCore}}` | Nest runtime |
| `@nestjs/platform-fastify` | `{{stack.nestPlatformFastify}}` | Fastify HTTP adapter |
{{#if swagger}}| `@nestjs/swagger` | `{{stack.nestSwagger}}` | OpenAPI document generation |
{{/if}}| `@nestjs/typeorm` | `{{stack.nestTypeorm}}` | Nest integration for TypeORM |
| `pg` | `{{stack.pg}}` | PostgreSQL driver |
{{#if auth}}| `@nestjs/jwt` | `{{stack.nestJwt}}` | JWT token issuing/verification |
| `@nestjs/passport` | `{{stack.nestPassport}}` | Nest auth integration |
| `passport-jwt` | `{{stack.passportJwt}}` | JWT strategy |
{{/if}}| `class-validator` | `{{stack.classValidator}}` | Request DTO validation |
| `class-transformer` | `{{stack.classTransformer}}` | DTO transformation |
| `jest` | `{{stack.jest}}` | Test runner |

## Operational Notes

{{#if docker}}- The development database scaffold uses `docker-compose.yml` for PostgreSQL
{{else}}- This project does not include Docker scaffolding, so PostgreSQL must be provided separately
{{/if}}- The scaffold always uses PostgreSQL + TypeORM as its persistence baseline
{{#if seeds}}- A seed runner is available at `src/database/seed.ts`
{{else}}- No seed runner is included in the current configuration
{{/if}}{{#if swagger}}- The Swagger endpoint is scaffolded through `src/common/swagger/setup-swagger.ts`
{{/if}}- The versions above come from the dependency baseline for this CLI release
