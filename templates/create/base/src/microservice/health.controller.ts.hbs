import { Controller } from '@nestjs/common';
import { GrpcMethod } from '@nestjs/microservices';

@Controller()
export class HealthController {
  @GrpcMethod('HealthService', 'Check')
  check(data: unknown): { status: string } {
    return { status: 'SERVING' };
  }
}
